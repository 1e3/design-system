{
  "name": "@1e3/design-system",
  "private": true,
  "scripts": {
    "build": "lerna run build --ignore=@1e3/docs --stream",
    "dev": "lerna run --parallel dev"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}